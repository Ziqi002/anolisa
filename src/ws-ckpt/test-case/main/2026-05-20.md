# 2026-05-20 — main 分支 ws-ckpt 全量初始化测试

> 状态：**待审核**

## 覆盖 commits（main 全部 ws-ckpt 历史，按时间正序）

| SHA | 标题 |
|-----|------|
| 683eb36 | feat(ckpt): introduce ws-ckpt into ANOLISA (#343) |
| 4f4e75a | feat(ckpt): add skill for openclaw |
| 09ba005 | chore(ci): adapt CI workflows for ws-ckpt component |
| 2248459 | chore(ckpt): rm build-rpm, ci will do it |
| de70b0d | fix(ckpt): fix the backend detection and state recovery logic |
| d1e9213 | fix(ckpt): make image size config take effect after restart |
| 549570f | fix(ckpt): optimize health check skip condition |
| bd463a5 | chore(ckpt): fix lint warn |
| 03e8fc7 | fix(ckpt): reconcile error cannot block daemon start |
| dfdcde0 | feat(ckpt): add auto_cleanup switch + push-based schedule |
| 62b7f3c | feat(ckpt): auto_cleanup_keep support count or duration |
| 02a113a | feat(ckpt): unify config modify entry as file + reload |
| a632430 | feat(ckpt): add ws-ckpt config entry for auto-clean |
| f906964 | fix(ckpt): remove fs_warn_threshold_percent |
| 0f4dd1f | feat(ckpt): post-command health advisory |
| 4eccdb2 | fix(ckpt): config.toml ships as sample |
| 7c3be59 | fix(ckpt): use restart in spec %post |
| 49376a1 | fix(ckpt): parse_duration_secs rejects > i64::MAX |
| 1eecd08 | chore(ckpt): bump version to 0.2.0 |
| dffa3a4 | feat(ckpt): change daemon to stateful (state.json) |
| 070be6a | feat(ckpt): mv state file img to /var/lib/ws-ckpt/ |
| e927b05 | feat(ckpt): rm placeholder overlayFS backend |
| 01f801f | fix(ckpt): pre-stage in spec shouldn't block install |
| 77c0e51 | refactor(ckpt): bootstrap as trait |
| 61d5969 | fix(ckpt): move legacy img migration into daemon bootstrap |
| f548458 | fix(ckpt): drop dead img_path from DaemonConfig/ConfigReport |

## 为什么加

初次触发，需要对 main 分支上 ws-ckpt 的全部历史做一次基线覆盖。确保项目骨架完整、RPM 打包契约正确、配置文件字段与废弃字段同步、systemd 单元正确声明、源码结构符合重构后预期、CLI 接口符合设计、daemon 端到端链路可用。

---

## 测试用例详细说明

### Section 1 — 项目骨架 (关联 commit: 683eb36)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 1.1 | skeleton: Cargo.toml | 683eb36 引入 ws-ckpt workspace，Cargo.toml 是 Rust workspace 的入口文件，缺失则整个项目无法编译 | 检查 `src/ws-ckpt/src/Cargo.toml` 文件存在 | PASS — 文件存在 |
| 1.2 | skeleton: cli/Cargo.toml | CLI crate 是用户唯一入口，缺失说明 crate 骨架损坏 | 检查 `crates/cli/Cargo.toml` 存在 | PASS — 文件存在 |
| 1.3 | skeleton: daemon/Cargo.toml | Daemon crate 负责后台服务，缺失则 daemon 编译失败 | 检查 `crates/daemon/Cargo.toml` 存在 | PASS — 文件存在 |
| 1.4 | skeleton: common/Cargo.toml | Common crate 包含共享类型定义（IPC 协议、配置结构），缺失则其他 crate 无法引用公共类型 | 检查 `crates/common/Cargo.toml` 存在 | PASS — 文件存在 |
| 1.5 | skeleton: README.md | 项目文档入口，开源合规要求 | 检查 `ws-ckpt/README.md` 存在 | PASS — 文件存在 |
| 1.6 | skeleton: CHANGELOG.md | 版本历史记录，发布流程需要 | 检查 `ws-ckpt/CHANGELOG.md` 存在 | PASS — 文件存在 |
| 1.7 | skeleton: LICENSE | 开源合规硬要求 | 检查 `ws-ckpt/LICENSE` 存在 | PASS — 文件存在 |
| 1.8 | skeleton: spec file | RPM 打包入口模板，缺失则 CI 无法产出 RPM | 检查 `ws-ckpt.spec.in` 存在 | PASS — 文件存在 |

### Section 2 — Skill 文件 (关联 commit: 4f4e75a)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 2.1 | SKILL.md present | 4f4e75a 新增 skill 描述文件，供 agent 框架发现并调用 ws-ckpt 能力 | 检查 `skills/ws-ckpt/SKILL.md` 文件存在 | PASS — 文件存在 |
| 2.2 | skill references ws-ckpt checkpoint | SKILL.md 必须引导 agent 知道 `checkpoint` 操作，否则 agent 无法调用核心功能 | 读取 SKILL.md 内容并检查包含 "ws-ckpt checkpoint" 字样 | PASS — 包含该字符串 |
| 2.3 | skill references ws-ckpt rollback | SKILL.md 必须引导 agent 知道 `rollback` 操作 | 读取 SKILL.md 内容并检查包含 "ws-ckpt rollback" 字样 | PASS — 包含该字符串 |
| 2.4 | skill references ws-ckpt list | SKILL.md 必须引导 agent 知道 `list` 操作 | 读取 SKILL.md 内容并检查包含 "ws-ckpt list" 字样 | PASS — 包含该字符串 |

### Section 3 — RPM Spec 契约 (关联 commits: 09ba005, 7c3be59, 01f801f, 4eccdb2)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 3.1 | spec: Source1 vendor.tar.gz | RPM 离线构建必须有 vendor 源；缺失则 CI 构建时网络隔离环境下 cargo 无法获取依赖 | 检查 spec 文件内容包含 "vendor.tar.gz" | PASS — 包含 |
| 3.2 | spec: BuildRequires cargo | Spec 必须声明 cargo 为构建依赖，否则 mock/koji 环境无法编译 | 检查 spec 内容包含 "cargo" | PASS — 包含 |
| 3.3 | spec: %{_bindir}/ws-ckpt | 09ba005 约定最终二进制安装到标准 `/usr/bin/`（即 `%{_bindir}`），而非 `/usr/local/bin` | 检查 spec 包含 `%{_bindir}/ws-ckpt` | PASS — 包含 |
| 3.4 | spec: no /usr/local/bin | 确保没有把二进制安装到非标准路径 | 检查 spec **不**包含 "/usr/local/bin/ws-ckpt" | PASS — 不包含 |
| 3.5 | spec: cargo --offline | 网络隔离构建要求 `--offline` 标志，否则在无网络环境会失败 | 检查 spec 包含 "--offline" | PASS — 包含 |
| 3.6 | spec: no modprobe btrfs | 01f801f 移除了 %pre 中的 modprobe btrfs 调用，因为它会阻塞安装（在无 btrfs 模块的环境） | 检查 spec **不**包含 "modprobe btrfs" | PASS — 不包含 |
| 3.7 | spec: no overlayfs | e927b05 移除了 overlayFS 后端，spec 中不应再有相关引用 | 检查 spec **不**包含 "overlayfs" | PASS — 不包含 |
| 3.8 | spec: config.toml.sample | 4eccdb2 把配置文件改为 sample 发布（不覆盖用户已有配置），安装路径必须是 `/etc/ws-ckpt/config.toml.sample` | 检查 spec 包含 "/etc/ws-ckpt/config.toml.sample" | PASS — 包含 |
| 3.9 | spec: %post uses systemctl restart | 7c3be59 修复：%post 脚本使用 `systemctl restart ws-ckpt` 以确保升级后新版 daemon 生效 | 用正则检查 spec 中存在 `systemctl restart ws-ckpt` 模式 | PASS — 匹配到 |

### Section 4 — Config Sample 字段 (关联 commits: d1e9213, dfdcde0, 62b7f3c, f906964)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 4.1 | sample: img_size | d1e9213 改为通过 `img_size` 字符串配置镜像大小（如 "50G"），此字段必须存在 | 检查 config.toml.sample 包含 "img_size" | PASS — 包含 |
| 4.2 | sample: img_max_percent | d1e9213 新增磁盘占比上限配置项，防止镜像吞掉全部磁盘 | 检查包含 "img_max_percent" | PASS — 包含 |
| 4.3 | sample: no img_min_size_gb | d1e9213 废弃旧字段 `img_min_size_gb`（用 img_size 取代），若仍存在会给用户造成困惑 | 检查 **不**包含 "img_min_size_gb" | PASS — 不包含 |
| 4.4 | sample: no img_capacity_pct | 同上，废弃字段 `img_capacity_percent` | 检查 **不**包含 "img_capacity_percent" | PASS — 不包含 |
| 4.5 | sample: auto_cleanup | dfdcde0 引入自动清理开关，配置文件必须有此字段供用户启停 | 检查包含 "auto_cleanup" | PASS — 包含 |
| 4.6 | sample: auto_cleanup_keep | 62b7f3c 引入保留策略字段（按个数或时长），用户需要可配置保留规则 | 检查包含 "auto_cleanup_keep" | PASS — 包含 |
| 4.7 | sample: no fs_warn_threshold | f906964 移除了 `fs_warn_threshold_percent` 字段（功能合并入 HealthAdvisory），残留会误导用户 | 检查 **不**包含 "fs_warn_threshold_percent" | PASS — 不包含 |
| 4.8 | sample: no overlayfs | e927b05 移除 overlayFS 后端后配置中不应有残留引用 | 检查 **不**包含 "overlayfs" | PASS — 不包含 |

### Section 5 — systemd Unit (关联 commits: 02a113a, dffa3a4)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 5.1 | unit: StateDirectory=ws-ckpt | dffa3a4 引入 state.json 持久化，systemd 的 `StateDirectory` 确保 `/var/lib/ws-ckpt` 自动创建和权限管理 | 检查 service 文件包含 "StateDirectory=ws-ckpt" | PASS — 包含 |
| 5.2 | unit: RuntimeDirectory=ws-ckpt | Daemon 使用 `/run/ws-ckpt/ws-ckpt.sock` 做 IPC，`RuntimeDirectory` 确保运行时目录存在 | 检查包含 "RuntimeDirectory=ws-ckpt" | PASS — 包含 |
| 5.3 | unit: ExecReload= ws-ckpt reload | 02a113a 统一配置修改入口为"文件修改 + reload"，ExecReload 必须指向 `ws-ckpt reload` 子命令 | 用正则检查 ExecReload 行包含 `ws-ckpt reload` 模式 | PASS — 匹配到 |

### Section 6 — 源码结构性断言 (关联 commits: 77c0e51, dffa3a4, 070be6a, 61d5969, f548458, 0f4dd1f, e927b05, 02a113a, dfdcde0, 62b7f3c, 49376a1, 01f801f)

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 6.1 | no bootstrap.rs | 77c0e51 将 bootstrap 从独立文件重构为 trait default method，旧文件应被删除 | 检查 `daemon/src/bootstrap.rs` 文件**不存在** | PASS — 文件不存在 |
| 6.2 | no overlayfs.rs | e927b05 移除 overlayFS 后端占位，对应模块文件必须删除 | 检查 `daemon/src/backends/overlayfs.rs` **不存在** | PASS — 文件不存在 |
| 6.3 | common: persist module | dffa3a4 引入 state.json 序列化，persist 模块负责状态持久化 | 在 common/src 中查找 `persist.rs` 文件或 `mod persist` 声明 | PASS — 模块存在 |
| 6.4 | common: migration module | 070be6a/61d5969 将 img 从 /data/ 迁移到 /var/lib/，migration 模块负责迁移逻辑 | 在 common/src 中查找 `migration.rs` 或 `mod migration` | PASS — 模块存在 |
| 6.5 | daemon: lockfile module | 避免多个 daemon 实例同时操作 btrfs，lockfile 模块实现互斥锁 | 在 daemon/src 中查找 `lockfile.rs` 或 `mod lockfile` | PASS — 模块存在 |
| 6.6 | daemon: util module | 公共工具函数（如 is_mounted）抽取到 util | 在 daemon/src 中查找 `util.rs` 或 `mod util` | PASS — 模块存在 |
| 6.7 | common: bootstrap trait method | 77c0e51 重构为 trait + default impl，后端只需 impl 特定方法 | grep common/src 中存在 `fn bootstrap` 声明 | PASS — 找到 |
| 6.8 | daemon: is_mounted helper | Daemon 需要判断 btrfs image 是否已挂载，避免重复 mount | grep daemon/src 中存在 `fn is_mounted` 或 `async fn is_mounted` | PASS — 找到 |
| 6.9 | BTRFS_IMG_PATH = /var/lib/ws-ckpt/btrfs-data.img | 070be6a 确定新路径常量值，FHS 合规（/var/lib 存放可变数据） | grep common/src 确认 BTRFS_IMG_PATH 值为 `/var/lib/ws-ckpt/btrfs-data.img` | PASS — 值正确 |
| 6.10 | LEGACY_BTRFS_IMG_PATH = /data/ws-ckpt/btrfs-data.img | 61d5969 迁移需要知道旧路径以做 mv/检测 | grep common/src 确认 LEGACY_BTRFS_IMG_PATH 值为 `/data/ws-ckpt/btrfs-data.img` | PASS — 值正确 |
| 6.11 | common/lib.rs: img_path removed | f548458 从 DaemonConfig/ConfigReport 中移除 img_path 字段（路径改为常量，无需配置） | grep `common/src/lib.rs` 中不含非注释的 `img_path` | PASS — 已移除 |
| 6.12 | common: HealthAdvisory variant | 0f4dd1f 新增 post-command 健康建议，IPC 响应枚举需包含此 variant | grep common/src 中存在 `HealthAdvisory` | PASS — 找到 |
| 6.13 | common: ReloadConfig variant | 02a113a 统一配置修改为 file+reload，IPC 命令枚举需有 `ReloadConfig` | grep common/src 中存在 `ReloadConfig` | PASS — 找到 |
| 6.14 | daemon: SIGHUP handler | 02a113a 实现：systemd `ExecReload` 发 SIGHUP → daemon 重载配置 | grep daemon/src 中存在 `SIGHUP` 或 `SignalKind::hangup` | PASS — 找到 |
| 6.15 | daemon: scheduler uses Notify | dfdcde0 将定时清理从 sleep-loop 改为 tokio Notify push 模型，减少无效唤醒 | grep daemon/src 中存在 `Notify` | PASS — 找到 |
| 6.16 | common: CleanupRetention enum | 62b7f3c 允许 auto_cleanup_keep 为数量或时长，需要 enum 区分两种语义 | grep common/src 中存在 `CleanupRetention` | PASS — 找到 |
| 6.17 | common: parse_duration_secs | 62b7f3c/49376a1 解析如 "7d"/"24h" 字符串为秒数，并拒绝超 i64::MAX 的异常值 | grep common/src 中存在 `fn parse_duration_secs` | PASS — 找到 |
| 6.18 | common: validate_file_config | 02a113a 配置改为文件写入后 reload，需要验证函数确保文件内容合法后才允许重载 | grep common/src 中存在 `fn validate_file_config` | PASS — 找到 |
| 6.19 | daemon: ensure_btrfs_support | 01f801f 把 btrfs 预检从 spec %pre 移入 daemon 启动流程，避免安装阻塞 | grep daemon/src 中存在 `ensure_btrfs_support` | PASS — 找到 |
| 6.20 | daemon: decide_effective_img_path | 61d5969 迁移决策函数：若 legacy 路径有文件则 mv 到新路径，否则用新路径 | grep daemon/src 中存在 `decide_effective_img_path` | PASS — 找到 |
| 6.21 | Cargo.toml: version 0.2.x | 1eecd08 bump 版本到 0.2.0，确认版本号已生效 | grep 所有 Cargo.toml 中存在 `version = "0.2.` | PASS — 找到 |

### Section 7 — CLI 行为测试 (需要 ws-ckpt 二进制)

> 前置条件：系统中已安装或编译了 ws-ckpt 二进制。若无二进制则整节 SKIP。

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 7.1 | help: init | CLI 必须暴露所有子命令，`--help` 是用户发现功能的入口 | 执行 `ws-ckpt --help`，检查输出包含 "init" | PASS — 包含 |
| 7.2 | help: checkpoint | 同上 | 检查 --help 输出包含 "checkpoint" | PASS — 包含 |
| 7.3 | help: rollback | 同上 | 检查包含 "rollback" | PASS — 包含 |
| 7.4 | help: delete | 同上 | 检查包含 "delete" | PASS — 包含 |
| 7.5 | help: list | 同上 | 检查包含 "list" | PASS — 包含 |
| 7.6 | help: diff | 同上 | 检查包含 "diff" | PASS — 包含 |
| 7.7 | help: cleanup | 同上 | 检查包含 "cleanup" | PASS — 包含 |
| 7.8 | help: status | 同上 | 检查包含 "status" | PASS — 包含 |
| 7.9 | help: config | 同上 | 检查包含 "config" | PASS — 包含 |
| 7.10 | help: reload | 同上 | 检查包含 "reload" | PASS — 包含 |
| 7.11 | version contains 0.2 | 1eecd08 版本号应与 Cargo.toml 一致 | 执行 `ws-ckpt --version`，检查输出包含 "0.2." | PASS — 包含 |
| 7.12 | config flag: --img-size | d1e9213 引入的配置 flag | 执行 `ws-ckpt config --help`，检查输出包含 "--img-size" | PASS — 包含 |
| 7.13 | config flag: --img-max-percent | 同上 | 检查包含 "--img-max-percent" | PASS — 包含 |
| 7.14 | config flag: --enable-auto-cleanup | dfdcde0 引入自动清理开关 | 检查包含 "--enable-auto-cleanup" | PASS — 包含 |
| 7.15 | config flag: --disable-auto-cleanup | 同上 | 检查包含 "--disable-auto-cleanup" | PASS — 包含 |
| 7.16 | config flag: --auto-cleanup-keep | 62b7f3c 引入保留策略 flag | 检查包含 "--auto-cleanup-keep" | PASS — 包含 |
| 7.17 | config flag: --auto-cleanup-interval | 同上 | 检查包含 "--auto-cleanup-interval" | PASS — 包含 |
| 7.18 | rejects --img-min-size-gb | d1e9213 废弃此旧 flag，CLI 应拒绝并报错 | 执行 `ws-ckpt config --img-min-size-gb 2`，检查退出码非零 | PASS — 非零退出 |
| 7.19 | rejects --img-capacity-percent | 同理废弃 flag | 执行 `ws-ckpt config --img-capacity-percent 80`，检查退出码非零 | PASS — 非零退出 |
| 7.20 | rejects --fs-warn-threshold-percent | f906964 移除此字段后 CLI 也不应接受 | 执行 `ws-ckpt config --fs-warn-threshold-percent 80`，检查退出码非零 | PASS — 非零退出 |
| 7.21 | enable+disable mutual exclusion | 逻辑互斥：不能同时启停自动清理 | 执行 `ws-ckpt config --enable-auto-cleanup --disable-auto-cleanup`，检查退出码非零 | PASS — 非零退出 |
| 7.22 | rejects --auto-cleanup-keep 30y | 49376a1 修复：duration 解析拒绝不合理值（30年秒数溢出 i64） | 执行 `ws-ckpt config --auto-cleanup-keep "30y"`，检查退出码非零 | PASS — 非零退出 |
| 7.23 | init rejects empty workspace | 健壮性：空字符串 workspace 路径无意义，必须拒绝 | 执行 `ws-ckpt init -w ""`，检查退出码非零 | PASS — 非零退出 |
| 7.24 | init rejects / as workspace | 健壮性：根目录作为 workspace 极其危险（rollback 会清除根目录内容） | 执行 `ws-ckpt init -w "/"`，检查退出码非零 | PASS — 非零退出 |

### Section 8 — Daemon 集成测试 (需要 root + btrfs + ws-ckpt service)

> 前置条件：Linux root + btrfs 文件系统 + ws-ckpt.service 已安装。不满足则整节 SKIP。

| # | 用例名 | 为什么测 | 干了什么 | 期望结果 |
|---|--------|---------|---------|---------|
| 8.1 | init workspace | ws-ckpt 核心功能入口：初始化 workspace 并在 daemon 中注册 | `systemctl start ws-ckpt` 后执行 `ws-ckpt init -w /tmp/ws-ckpt-qa-$$` | PASS — 退出码 0 |
| 8.2 | checkpoint qa1 | 核心功能：创建第一个快照 | 执行 `ws-ckpt checkpoint -w <ws> -i qa1 -m "qa test"` | PASS — 退出码 0 |
| 8.3 | checkpoint qa2 | 验证连续快照能力：先写入文件变更再建立第二个快照 | 写入 test.txt 后执行 `ws-ckpt checkpoint -w <ws> -i qa2 -m "second"` | PASS — 退出码 0 |
| 8.4 | list exit 0 | list 子命令应正常返回已有快照列表 | 执行 `ws-ckpt list -w <ws>` | PASS — 退出码 0 |
| 8.5 | list shows qa1 | list 输出必须包含已创建的快照 ID | 检查 list 输出包含 "qa1" | PASS — 包含 |
| 8.6 | list shows qa2 | 同上 | 检查 list 输出包含 "qa2" | PASS — 包含 |
| 8.7 | list json exit 0 | list 支持 `--format json` 结构化输出，供程序解析 | 执行 `ws-ckpt list -w <ws> --format json` | PASS — 退出码 0 |
| 8.8 | json output contains qa1 | JSON 格式输出同样需要包含快照信息 | 检查 json 输出包含 "qa1" | PASS — 包含 |
| 8.9 | diff qa1..qa2 exit 0 | diff 显示两个快照间文件变更，验证 btrfs send/receive diff 机制 | 执行 `ws-ckpt diff -w <ws> -f qa1 -t qa2` | PASS — 退出码 0 |
| 8.10 | diff shows test.txt | qa1→qa2 之间新增了 test.txt，diff 应报告该文件 | 检查 diff 输出包含 "test.txt" | PASS — 包含 |
| 8.11 | diff rejects unknown snapshot | 对不存在的快照 ID 做 diff 应报错而非崩溃 | 执行 `ws-ckpt diff -w <ws> -f nosuch -t qa2`，检查退出码非零 | PASS — 非零退出 |
| 8.12 | rollback to qa1 exit 0 | 核心功能：回滚到指定快照恢复工作区状态 | 执行 `ws-ckpt rollback -w <ws> -s qa1` | PASS — 退出码 0 |
| 8.13 | file removed after rollback | 验证 rollback 实际效果：qa1 时 test.txt 不存在，回滚后该文件应消失 | 检查 `<ws>/test.txt` 文件不存在 | PASS — 文件不存在 |
| 8.14 | status exit 0 | status 子命令展示 workspace 当前状态信息 | 执行 `ws-ckpt status -w <ws>` | PASS — 退出码 0 |
| 8.15 | config view exit 0 | config 不带参数显示当前配置 | 执行 `ws-ckpt config` | PASS — 退出码 0 |
| 8.16 | config: no Image path: | f548458 移除 img_path 后，config 输出中不应再显示该字段 | 检查 config 输出 **不**包含 "Image path:" | PASS — 不包含 |
| 8.17 | reload exit 0 | 02a113a 新增 reload 子命令，触发 daemon 重新加载配置 | 执行 `ws-ckpt reload` | PASS — 退出码 0 |
| 8.18 | SIGHUP does not kill daemon | 02a113a 安装了 SIGHUP handler，收到信号应重载配置而非终止进程 | 获取 daemon PID，发送 `kill -HUP`，1秒后检查进程仍存活 | PASS — 进程存活 |
| 8.19 | cleanup (teardown) | 测试结束清理：删除测试 workspace 和临时文件 | 执行 `ws-ckpt delete -w <ws> --force` + `rm -rf` | 清理完成 |

---

## 期待结果总结

| 环境 | 预期 |
|------|------|
| Mac（无二进制） | Section 1-6 全部 PASS（53 项静态断言），Section 7-8 SKIP |
| Linux + 已编译 ws-ckpt（无 root/btrfs） | Section 1-7 全部 PASS，Section 8 SKIP |
| Linux + root + btrfs + ws-ckpt.service | 全部 PASS |
